# Implement audit — iteration 1 — phase 4 — 2026-05-13-1923
> spec.md SHA: 53665556063768d6ef1e66b01bbf1c4eacd745e9
> Phase scope: AC12.1, AC12.2, AC12.3, AC12.4, AC12.5

## AC12.1 — Toggle button opens/closes left-side outline panel
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:2061–2101` — `ActivityBar` struct renders a 40pt vertical strip with a `list.bullet.indent` button bound to `onToggleOutline`; active state highlighted with accent color.
- `ContentView.swift:148–159` — `body` wraps the editor in `HStack { ActivityBar, (if showOutline) OutlineView, MarkdownEditor }`; panel appears at leading edge.
- `ContentView.swift:319–332` — `toggleOutline()` flips `showOutline` with save/restore side-effects.
- `markthisdownApp.swift:180–183` — "Toggle Outline View" menu item posts `.mtdToggleOutline` notification with `⌘⇧O` shortcut.
- `ContentView.swift:314–316` — `onReceive(.mtdToggleOutline)` calls `toggleOutline()`.
**Reason:** Toggle button exists in ActivityBar, keyboard shortcut is wired, panel opens and closes reactively from `showOutline` state.

## AC12.2 — Outline entries show heading level (indented) and text
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:2166–2193` — `OutlineRow` renders heading text with `.padding(.leading, CGFloat(heading.level - 1) * 12 + 10)` for per-level indent; font weight scales: h1 `.semibold`, h2 `.medium`, h3+ `.regular`.
- `ContentView.swift:2106–2163` — `OutlineView` iterates `headings` via `ForEach`, passes each to `OutlineRow`; shows "No headings yet." empty state.
- `ContentView.swift:1987–1991` — `MTDHeading` carries `level: Int` (1–6) and `text: String`.
**Reason:** Per-level indent (12pt per level) and weight differentiation are implemented; text is displayed verbatim.

## AC12.3 — Clicking an outline entry scrolls editor to heading and moves cursor
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:334–340` — `jumpToHeading(_:)` sets `jumpToken = EditorJumpToken(location: h.location, nonce: ...)` and sets `outlineJumped = true`.
- `ContentView.swift:1201–1210` — `updateNSView` applies the jump after `applyHighlighting`: calls `tv.setSelectedRange(r)` then `tv.scrollRangeToVisible(r)` (moves cursor and scrolls).
- `ContentView.swift:154–157` — `OutlineView(onSelect: jumpToHeading)` wires the row tap to `jumpToHeading`.
**Reason:** Reuses the existing `EditorJumpToken` pipeline; cursor move and scroll-to-visible are both executed.

## AC12.4 — Opening saves scroll position; closing restores it (unless jump occurred)
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:319–332` — `toggleOutline()`: on open calls `scrollBridge.save()` then sets `showOutline = true`; on close, if `outlineJumped` calls `scrollBridge.discard()`, else calls `scrollBridge.restore()`, then resets `outlineJumped = false`.
- `ContentView.swift:2041–2057` — `EditorScrollBridge`: `save()` captures `sv.contentView.bounds.origin`; `restore()` calls `sv.contentView.scroll(to: pt)` + `sv.reflectScrolledClipView`; `discard()` clears saved origin.
- `ContentView.swift:1149–1154` — `makeNSView` wires the `NSScrollView` into `scrollBridge.scrollView`.
- `ContentView.swift:1183` — `updateNSView` keeps `scrollBridge.scrollView` current if the scroll view instance changes.
**Reason:** Save/restore is correctly bracketed around open/close; jump-overrides-restore logic via `outlineJumped` flag is implemented.

## AC12.5 — Outline list updates when headings change
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:288–290` — `onChange(of: document.text)` handler calls `headings = parseHeadings(newText)` on every text change (same hook as comment parse).
- `ContentView.swift:284–286` — `onAppear` seeds `headings = parseHeadings(document.text)`.
- `ContentView.swift:1994–2019` — `parseHeadings(_:)` runs ATX regex `^(#{1,6})[ \t]+(.+?)[ \t]*#*[ \t]*$` with `.anchorsMatchLines`, filtering out matches inside frontmatter or fenced code blocks.
**Reason:** Headings are re-parsed synchronously on every `document.text` change; the `@State headings` binding drives `OutlineView` to re-render automatically.

---

## Invariant regression check

### Invariant 1 — Comments stored as plain `<!-- -->` HTML
**Verdict:** PASS
**Evidence:** No changes to `MTDComment` storage format or document write path. `ContentView.swift:289` still calls `MTDComment.parse(newText)`.
**Reason:** Outline feature adds no new document format; comment storage is untouched.

### Invariant 2 — Document extension is `.md` only
**Verdict:** skipped — orthogonal to phase scope

### Invariant 3 — Document remains LLM-readable without custom tooling
**Verdict:** skipped — orthogonal to phase scope

### Invariant 4 — Autosave is AppKit-supplied
**Verdict:** skipped — orthogonal to phase scope

### Invariant 5 — Comment margin icons visible in rendered mode
**Verdict:** PASS
**Evidence:** `applyHighlighting` path (`ContentView.swift:1360–1414`) and `ReadingTextView` draw path are unmodified; margin icon drawing code is not touched by the phase 4 diff.
**Reason:** No changes to the icon draw path or comment-range attribute marking.

### Invariant 6 — Comment insert/edit/delete/navigate UX preserved
**Verdict:** PASS
**Evidence:** Comment card interactions, blur-commit, Esc-revert, Enter-confirm paths in `ContentView.swift` are not modified by the phase 4 commit. `jumpToken` usage for sidebar card jumps (`ContentView.swift:182–185`) coexists with the new `jumpToHeading` usage.
**Reason:** Outline adds a parallel use of `EditorJumpToken`; existing comment UX paths are unmodified.

### Invariant 7 — Raw mode shows plain comment text and margin icons
**Verdict:** PASS
**Evidence:** `ContentView.swift:1389–1398` raw-mode branch still marks `mtdComment` / `mtdCommentLocation` attributes; `ReadingTextView` draw path unchanged.
**Reason:** Phase 4 does not touch the raw-mode branch of `applyHighlighting`.

### Invariant 8 — Comments inside triple-backtick fences excluded from parsing/hiding/icons
**Verdict:** PASS
**Evidence:** `MTDComment.parse` and fence-exclusion logic not modified by phase 4 commit. Separately, `parseHeadings` also correctly filters out fenced-code matches (`ContentView.swift:2011`).
**Reason:** Fence exclusion for comments is unmodified; outline parsing correctly skips fences too.

### Invariant 9 — Scroll position preserved across `applyHighlighting` calls
**Verdict:** PASS
**Evidence:** `ContentView.swift:1370` saves `tv.enclosingScrollView?.contentView.bounds.origin`; `ContentView.swift:1408–1412` restores via `setBoundsOrigin` + `reflectScrolledClipView` after `storage.endEditing()`. This mechanism is unchanged by phase 4.
- The `EditorScrollBridge` (AC12.4) operates at the panel-open/close level, which is a different save/restore cycle from the per-highlight save. Both coexist without conflict: `scrollBridge` saves on panel open before any `updateNSView` cycle; `applyHighlighting` saves/restores on every highlight; the jump is applied after `applyHighlighting` (`ContentView.swift:1198–1210`) so the post-jump scroll position is not immediately undone by a subsequent highlight restore.
**Reason:** Per-highlight scroll preservation is intact; `EditorScrollBridge` coexists correctly because jump is applied after `applyHighlighting` in the same `updateNSView` pass.

### Invariant 10 — Theme cycling and font-zoom continue to work
**Verdict:** skipped — orthogonal to phase scope

### Provisional Invariant 1 — Comment parse is synchronous on every `document.text` change
**Verdict:** PASS — with note
**Evidence:** `ContentView.swift:288–290` — single `onChange(of: document.text)` handler now calls both `MTDComment.parse(newText)` and `parseHeadings(newText)` synchronously.
**Reason:** Adding `parseHeadings` to the same synchronous `onChange` hook is consistent with the provisional invariant's intent; it adds a second O(n) scan per keystroke. At typical document sizes this is acceptable per the invariant's stated condition; heading count is similarly bounded. No re-evaluation trigger is met by this change alone.

### Provisional Invariant 2 — Structural-line block-above insertion applies to list items
**Verdict:** skipped — orthogonal to phase scope

---

## Summary
Phase ACs: 5, PASS: 5, GAP: 0, UNCLEAR: 0
Invariants checked: 10 substantive + 2 provisional, regressions detected: 0
