# Implement audit — iteration 1 — phase 1 — 2026-05-12-2307
> spec.md SHA: a3769dacc1494c77101dbe934b2db883b22f1f8f
> Phase scope: AC1.1, AC1.2, AC2.1, AC2.2, AC3.1, AC3.2, AC4.1, AC4.2, AC5.1, AC6.1, AC6.2, AC6.3, AC6.4, AC6.5, AC7.1, AC7.2

## AC1.1 — Info.plist CFBundleShortVersionString
**Verdict:** PASS
**Evidence:** `markthisdown/Info.plist:12` — `<string>2.0.12</string>` under the `CFBundleShortVersionString` key.
**Reason:** The plist key reads exactly "2.0.12" as required.

## AC1.2 — Toolbar version label displays "v2.0.12 · <zoom>%"
**Verdict:** PASS
**Evidence:** `ContentView.swift:19-22` — `appVersion` computed property reads `CFBundleShortVersionString` and prepends "v"; `ContentView.swift:195` — `Text("\(appVersion) · \(zoomLabel)")` in the toolbar; `ContentView.swift:276-278` — `zoomLabel` returns `"\(pct)%"`.
**Reason:** The toolbar text is composed as "v\(CFBundleShortVersionString) · \(zoom)%" which matches the AC format.

## AC2.1 — `comments` is a `@State` variable updated by a single `onChange` handler on `document.text`
**Verdict:** PASS
**Evidence:** `ContentView.swift:106` — `@State private var comments: [MTDComment] = []`; `ContentView.swift:248-249` — single `.onChange(of: document.text)` handler that sets `comments = MTDComment.parse(newText)`. No other site sets `comments` except `.onAppear` (line 245), which is not part of the cyclic highlight path.
**Reason:** Exactly one `onChange` on `document.text` drives comment parsing, satisfying the single-parse-per-change requirement.

## AC2.2 — No duplicate parse occurs during a single `applyHighlighting` call cycle
**Verdict:** PASS
**Evidence:** `ContentView.swift:1135-1189` — `applyHighlighting` reads `parent.commentRanges` (a pre-computed `[NSRange]` passed in) and never calls `MTDComment.parse`. The `comments` state is set only in `.onChange(of: document.text)` (`ContentView.swift:249`) and `.onAppear` (`ContentView.swift:245`), neither of which is invoked from within `applyHighlighting`.
**Reason:** `applyHighlighting` consumes already-parsed comment ranges; it does not trigger a second parse.

## AC3.1 — `CommentsSidebar` contains no search field or filter state
**Verdict:** PASS
**Evidence:** `ContentView.swift:333-419` — `CommentsSidebar` struct has no `@State` variables at all; its parameters are `documentText`, `comments`, `focusedCommentLocation`, and `onAddComment`. No `TextField`, `searchable`, or filter binding is present. Grep for "searchText", "filter", "searchField" returns no matches in ContentView.swift.
**Reason:** The sidebar struct carries no search or filter state of any kind.

## AC3.2 — All comments are shown in position order with no filter applied
**Verdict:** PASS
**Evidence:** `ContentView.swift:372-384` — `ForEach(comments)` iterates the full `comments` array without filtering; `comments` is sorted by parse order which is document position order (regex enumeration from start, `ContentView.swift:43-65`).
**Reason:** All comments from the parse result are displayed in document order, no filter applied.

## AC4.1 — After ⌘' inserts a new comment with sidebar open, the new comment card's body field receives keyboard focus without requiring a manual click
**Verdict:** PASS
**Evidence:** `ContentView.swift:267-272` — `.onReceive(.mtdCommentAdded)` sets `focusedCommentLocation = loc` and `showSidebar = true`; `ContentView.swift:376` — each `CommentCard` receives `isFocused: c.id == focusedCommentLocation`; `ContentView.swift:508-510` — `.task { if isFocused { bodyFocused = true } }` claims focus when the card first appears; `ContentView.swift:514-516` — `.onChange(of: isFocused)` also sets `bodyFocused = true` if the card was already visible.
**Reason:** `.task` and `.onChange(of: isFocused)` together ensure focus is programmatically claimed on the new card in both appear and update paths.

## AC4.2 — After ⌘' with sidebar closed, sidebar opens and the new card receives focus
**Verdict:** PASS
**Evidence:** `ContentView.swift:269` — `showSidebar = true` is set unconditionally in the `.onReceive(.mtdCommentAdded)` handler regardless of prior sidebar state; the same `focusedCommentLocation` assignment and `.task` / `.onChange(of: isFocused)` focus path (AC4.1 evidence) then fires as the new card appears.
**Reason:** The notification handler opens the sidebar and sets the focus target before the card even renders, so `.task` fires with `isFocused = true`.

## AC5.1 — Clicking the right-margin band where no comment icon is drawn produces no action
**Verdict:** PASS
**Evidence:** `ContentView.swift:608-618` — `mouseDown` calls `commentLocationForMarginIcon(at:)`; if it returns `nil` (no icon at that point), control falls through to `super.mouseDown(with: event)` — the standard NSTextView handler — which is a no-op for margin clicks; `ContentView.swift:627-658` — `commentLocationForMarginIcon` only returns a non-nil value when a comment's `lineBand` rect contains the click point; points in empty margin return `nil`.
**Reason:** The first-comment fallback has been removed; an empty hit falls through to default NSTextView behavior.

## AC6.1 — `ReadingTextView` shows a persistent toggle button whose label and action depend on document state
**Verdict:** PASS
**Evidence:** `ContentView.swift:215-220` — a `Button` is always present in the toolbar, calling `frontmatterButtonAction()` and labeled by `frontmatterButtonLabel`; `ContentView.swift:294-309` — label and help text are conditional on `hasFrontmatter` and `frontmatterCollapsed`. The AC also states "The button's visual form and placement are decided at implementation time", so placing it in the SwiftUI toolbar (rather than inside the NSTextView subclass) is within scope.
**Reason:** A permanent toolbar button exists whose label and action vary with document frontmatter state.

## AC6.2 — When no frontmatter block exists, the button reads "Add frontmatter"; activating it inserts a minimal `---\n---` block and leaves it expanded
**Verdict:** PASS
**Evidence:** `ContentView.swift:295` — `if !hasFrontmatter { return "Add frontmatter" }`; `ContentView.swift:288-292` — `insertFrontmatter()` inserts `"---\n---\n\n" + document.text` and sets `frontmatterCollapsed = false`.
**Reason:** Label is correct and insertion produces a minimal two-delimiter block in expanded state.

## AC6.3 — When a frontmatter block exists, the button reads "Collapse" or "Expand" depending on current collapse state
**Verdict:** PASS
**Evidence:** `ContentView.swift:296` — `return frontmatterCollapsed ? "Expand" : "Collapse"` (executes only when `hasFrontmatter` is true).
**Reason:** The label switches between "Collapse" and "Expand" exactly as specified.

## AC6.4 — Activating "Collapse" hides the frontmatter body lines; a single summary line remains visible
**Verdict:** PASS
**Evidence:** `ContentView.swift:1248-1270` — when `frontmatterCollapsed` is true, the code computes `hideStart` as the end of the opening `---` line and `hideEnd` as the end of the full frontmatter match, then applies `maximumLineHeight = 0.01`, `foregroundColor = clear`, and negative kern to that range, leaving the opening `---` line visible as a summary.
**Reason:** Body lines and closing delimiter are hidden via line-height/color tricks; the opening delimiter line is the sole remaining visible line.

## AC6.5 — Activating "Expand" restores all frontmatter lines; collapse/expand state does not modify `document.text`
**Verdict:** PASS
**Evidence:** `ContentView.swift:311-317` — `frontmatterButtonAction()` calls `frontmatterCollapsed.toggle()` when frontmatter exists; `frontmatterCollapsed` is a `@State` var that controls visual attributes only (`ContentView.swift:1248-1270`); `document.text` is never touched in the collapse/expand path.
**Reason:** Toggling `frontmatterCollapsed` is a pure display-layer operation; document.text is not modified.

## AC7.1 — With cursor inside a frontmatter block, ⌘' is a no-op
**Verdict:** PASS
**Evidence:** `ContentView.swift:823-836` — `mtdInsertCommentAction` checks `frontmatterRange(in: nsText as String)`; if the cursor location is within `fm.location…NSMaxRange(fm)`, or if the cursor's line range falls within the frontmatter span, the function returns early without inserting or posting any notification.
**Reason:** The guard returns early before any insertion or sidebar action when cursor is inside frontmatter.

## AC7.2 — With cursor on the `---` delimiter line, behavior matches AC7.1
**Verdict:** PASS
**Evidence:** `ContentView.swift:826-835` — the condition checks both `cursor >= fm.location && cursor <= fmEnd` (covers the opening `---` delimiter since the frontmatter match starts at the opening `---`) and `lineRangeOfCursor.location >= fm.location && lineRangeOfCursor.location < fmEnd` (covers any line whose start falls within the match, including the closing `---` line).
**Reason:** Both opening and closing `---` delimiter lines are within or adjacent to the frontmatter range and trigger the early return.

## Invariant regression check

### Invariant 1 — Comments as plain HTML
**Verdict:** PASS
**Evidence:** `ContentView.swift:33-67` — `MTDComment.parse` matches `<!--[\s\S]*?-->`; `ContentView.swift:400-410` — `updateBody` writes back `<!-- text -->` format; `markthisdownDocument.swift` (unchanged) writes UTF-8 `.md` files.
**Reason:** Comments are read and written as standard HTML comment syntax embedded in document text; no companion files or custom format.

### Invariant 2 — Document extension is `.md` only
**Verdict:** PASS
**Evidence:** `markthisdown/Info.plist:18-28` — `LSItemContentTypes` lists `public.markdown` and `net.daringfireball.markdown` only; no `.mtd` type. `markthisdownDocument.swift` (not modified by phase 1) writes `.md` only.
**Reason:** No `.mtd` or custom extension was introduced.

### Invariant 3 — Document remains LLM-readable without custom tooling
**Verdict:** PASS
**Evidence:** Comments remain plain `<!-- -->` HTML; frontmatter remains plain `---\n...\n---` YAML syntax; no binary data or custom encoding added in this phase.
**Reason:** Document content is standard Markdown + HTML comments; readable by any text tool.

### Invariant 4 — Autosave is AppKit-supplied; no custom save path
**Verdict:** PASS
**Evidence:** `ContentView.swift:248-257` — the `onChange` handler only updates a `saveState` enum for UI display and debounces a status label transition; actual persistence is via the `DocumentGroup` / `FileDocument` AppKit pipeline (unchanged). No custom `write(to:)` override added in this phase.
**Reason:** Phase 1 touches only UI state for save-status label; autosave mechanism is untouched.

### Invariant 5 — Comment margin icons visible in rendered mode alongside every comment
**Verdict:** PASS
**Evidence:** `ContentView.swift:1208-1213` — `SyntaxHighlighter.apply` tags every comment range with `.mtdComment = true` and `.mtdCommentLocation`; `ContentView.swift:786-815` — `drawCommentMarginIcons` enumerates those attributes and draws one `text.bubble` SF Symbol per unique Y position.
**Reason:** Comment ranges are marked and drawn as margin icons in rendered mode without modification.

### Invariant 6 — Existing comment insert / edit / delete UX preserved
**Verdict:** PASS
**Evidence:** `ContentView.swift:526-541` — `commentEditor` uses `TextField` with `.onKeyPress` handling Enter-commits and Shift/Cmd+Enter newlines; `ContentView.swift:472-481` — Esc via `.cancelAction` reverts; `ContentView.swift:413-418` — per-card delete; `ContentView.swift:517-522` — blur commits via `onChange(of: bodyFocused)`. The phase 1 commit message confirms these were preserved, and no changes to the card edit path are visible.
**Reason:** All five UX behaviors (blur-commit, Esc-revert, Enter-confirm, Shift/Cmd+Enter-newline, delete) are intact.

### Invariant 7 — Raw mode shows plain comment text and renders margin icons
**Verdict:** PASS
**Evidence:** `ContentView.swift:1164-1173` — in raw mode the code still tags comment ranges with `.mtdComment` and `.mtdCommentLocation` so margin icons draw; in raw mode text is not hidden, so comment source text is visible.
**Reason:** Raw mode icon tagging is preserved and comment text is not hidden in that path.

### Invariant 8 — Comments inside triple-backtick fences are excluded
**Verdict:** PASS
**Evidence:** `ContentView.swift:35-46` — `MTDComment.parse` calls `findFenceRanges` then skips any comment whose location falls inside a fence range; `ContentView.swift:1273-1285` — fenced-code blocks are styled separately; comment ranges produced by parse already exclude fenced matches.
**Reason:** Fence exclusion in `MTDComment.parse` is unchanged and still operative.

### Invariant 9 — Scroll position preserved across `applyHighlighting`
**Verdict:** PASS
**Evidence:** `ContentView.swift:1144-1145` — `savedScroll` captures `clipView.bounds.origin` before `beginEditing`; `ContentView.swift:1182-1188` — scroll is restored after `endEditing` if it drifted.
**Reason:** Save/restore of `clipView.bounds.origin` around the full storage rewrite is present and unchanged.

### Invariant 10 — Theme cycling and font-zoom continue to work
**Verdict:** PASS
**Evidence:** `markthisdownApp.swift:128-153` — `Cycle Theme` (⌘L), `Zoom In` (⌘=), `Zoom Out` (⌘-), `Actual Size` (⌘0) commands are all present and unmodified. `ContentView.swift:108-109` — `@AppStorage` for `themeRaw` and `fontScale` drive palette and scale; no changes to those bindings in phase 1.
**Reason:** Theme cycling and zoom commands are unmodified and their bindings remain intact.

### Provisional invariant — Comment parse is synchronous on every `document.text` change
**Verdict:** PASS
**Evidence:** `ContentView.swift:248-249` — `.onChange(of: document.text)` synchronously calls `comments = MTDComment.parse(newText)` with no `DispatchQueue.async`, `Task`, or `await`; the assignment completes before the closure returns.
**Reason:** Parse runs synchronously inline in the onChange handler; no deferred or async path.

## Summary
Phase ACs: 16, PASS: 16, GAP: 0, UNCLEAR: 0
Invariants checked: 11 (10 core + 1 provisional), regressions detected: 0
