# Implement audit — iteration 1 — phase 3 — 2026-05-13-1748
> spec.md SHA: a3769dacc1494c77101dbe934b2db883b22f1f8f
> Phase scope: AC9.1, AC9.2, AC10.1, AC10.2, AC10.3, AC11.1, AC11.2, AC11.3

## AC9.1 — GFM pipe table renders with visible column separators and header styling in rendered mode
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:1478-1487` — `applyTables(...)` is called from `SyntaxHighlighter.apply` only in rendered mode (guarded by the `if parent.mode == .rendered` branch at line 1327).
- `ContentView.swift:1770-1778` — `styleTable(...)` applies bold to the header row (`headerLineRange`) and sets monospace font + tinted background on the whole table.
- `ContentView.swift:1797-1811` — every `|` character outside the separator row is tagged `.mtdTablePipe` / `.mtdTablePipeColor` (hidden glyph, draw flag set).
- `ContentView.swift:811-837` — `drawTablePipes(in:)` iterates `.mtdTablePipe` attributes and draws a 1pt vertical line through each pipe's bounding rect, forming continuous column separators.
- `ContentView.swift:7-18` — `.mtdTablePipe` and `.mtdTablePipeColor` custom attribute keys defined.
- `ContentView.swift:617-628` — `draw(_:)` calls `drawTablePipes(in:)`.
**Reason:** Table detection, header bolding, separator-row collapse, and pipe drawing are all present and wired together.

## AC9.2 — Table source is hidden or styled consistently with other block elements in rendered mode
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:1770-1771` — whole table range receives monospace font and `codeBackground` tint (matching fenced code block treatment at line 1453-1454).
- `ContentView.swift:1782-1791` — separator row text color set to `NSColor.clear` and kern collapsed to hairline (same hide pattern used for HRs, comment lines, etc.).
- `ContentView.swift:1804-1808` — each `|` glyph hidden via `NSColor.clear` + negative kern; visual separator replaced by custom drawing.
- Comment in `ContentView.swift:1769` explicitly notes "matches code-block treatment".
**Reason:** Separator row and pipe glyphs are hidden; the whole table block uses the code-background tint consistent with other block-level hiding patterns.

## AC10.1 — Bare file paths are colored distinctly in rendered mode
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:1679-1684` — `pathPattern` regex matches tokens starting with `./`, `../`, `~/`, `/` (path prefixes) or ending with a `.ext` (1–6 char extension); colors them with `palette.pathColor`.
- `markthisdownApp.swift:82-83` (light) and `markthisdownApp.swift:104` (dark) — `pathColor` defined as a distinct teal color for both themes.
- `ContentView.swift:1668-1675` — `pathTagSkip` closure excludes fenced code, frontmatter, comments, inline code, Markdown links, and bare URLs — correctly scoping the rule.
- `ContentView.swift:1327` — the entire step 13 (path/tag coloring) runs inside the `if parent.mode == .rendered` block.
**Reason:** Pattern-based file-path detection with distinct per-theme color is implemented and active only in rendered mode.

## AC10.2 — `[bracket-tag]` tokens are colored distinctly; not followed by `(`
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:1687-1691` — pattern `\[([^\]\n]+)\](?!\()` uses negative lookahead to exclude `[text](` links; colors inner text with `palette.tagColor`.
- `markthisdownApp.swift:83-84` (light) and `markthisdownApp.swift:105` (dark) — `tagColor` defined as a distinct purple/violet color for both themes.
- Same `pathTagSkip` closure (line 1668-1675) applies, so bracket-tags are not colored inside fenced code, frontmatter, comments, inline code, or Markdown links.
**Reason:** Bracket-tag pattern with negative lookahead for `(` and distinct color is correctly implemented.

## AC10.3 — Coloring does not apply inside fenced code blocks or frontmatter
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:1464-1476` — `frontmatterRange` is computed and `fenceRanges` are collected before step 13.
- `ContentView.swift:1473-1476` — `isInFenceOrFrontmatter(r)` tests intersection with both `frontmatterRange` and `fenceRanges`.
- `ContentView.swift:1668-1669` — `pathTagSkip` delegates to `isInFenceOrFrontmatter` as its first check; both path and tag enumeration call `pathTagSkip` and return early when `true`.
**Reason:** Both path and tag coloring explicitly skip any range intersecting a fence or frontmatter block.

## AC11.1 — Raw mode shows line numbers in a reserved left margin for each logical line
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:973-1050` — `LineNumberRulerView: NSRulerView` iterates logical lines via `nsString.lineRange`, draws each line number right-aligned in a 44pt gutter.
- `ContentView.swift:1107-1112` — `LineNumberRulerView` is instantiated, attached to the scroll view as `verticalRulerView`, and made visible when `mode == .raw`.
- `ContentView.swift:1141-1146` — `updateNSView` keeps `scroll.rulersVisible` in sync with `mode == .raw` on every SwiftUI update cycle.
- `markthisdownApp.swift:19` — `lineNumberColor` field in `ThemePalette` drives per-theme gutter text color; set at line 1111 and kept updated at line 1144.
**Reason:** A full `NSRulerView` subclass draws line numbers in a 44pt left gutter, shown only in raw mode.

## AC11.2 — Wrapped logical lines: only first visual row is numbered; continuation rows show no number
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:1024-1049` — the draw loop uses `nsString.lineRange` (paragraph granularity) to obtain each logical line, then uses `lm.glyphIndexForCharacter(at: lineRange.location)` and `lm.lineFragmentRect(forGlyphAt:)` to get only the *first* line fragment's Y position. It increments `lineNumber` once per paragraph pass without drawing for continuation fragments.
- The comment at lines 1022-1023 explicitly states: "Only the first line fragment of each paragraph gets a number — wrapped continuation rows are skipped."
**Reason:** The ruler draws exactly one number per `lineRange` pass using the first fragment rect, so wrap-continuation rows are never numbered.

## AC11.3 — Line numbers are not visible in rendered mode
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:1110` — `scroll.rulersVisible = (mode == .raw)` in `makeNSView` initializes the gutter as hidden when starting in rendered mode.
- `ContentView.swift:1142` — `scroll.rulersVisible = (mode == .raw)` in `updateNSView` re-evaluates on every SwiftUI update, hiding the ruler when mode is `.rendered`.
**Reason:** `rulersVisible` is driven directly by `mode == .raw`; it is `false` in rendered mode.

## Invariant regression check

### Invariant 1 — Comments stored as plain `<!-- -->` HTML; no custom format, no companion files
**Verdict:** skipped — orthogonal to phase scope
**Reason:** No phase 3 code path modifies how comments are read from or written to the document.

### Invariant 2 — Document extension is `.md` only
**Verdict:** skipped — orthogonal to phase scope
**Reason:** Phase 3 does not touch file type declarations or document model.

### Invariant 3 — Document remains LLM-readable without custom tooling
**Verdict:** skipped — orthogonal to phase scope
**Reason:** Phase 3 adds only visual attributes to an `NSTextStorage`; it does not change the stored text.

### Invariant 4 — Autosave is AppKit-supplied; no custom save path
**Verdict:** skipped — orthogonal to phase scope
**Reason:** Phase 3 does not modify the save workflow.

### Invariant 5 — Comment margin icons visible in rendered mode alongside every comment
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:1383-1389` — `SyntaxHighlighter.apply` still tags all `commentRanges` with `.mtdComment` / `.mtdCommentLocation` before any phase 3 logic runs.
- `ContentView.swift:839-868` — `drawCommentMarginIcons` draws icons for every `.mtdComment`-attributed range.
**Reason:** Comment icon drawing path is untouched by phase 3 code; attribute tagging still runs before new steps.

### Invariant 6 — Existing comment insert / edit / delete UX is preserved
**Verdict:** PASS
**Evidence:** `ContentView.swift:872-968` — `mtdInsertCommentAction`, `computeCommentInsertion`, and the `CommentCard` / `CommentsSidebar` delete/update methods are unchanged by phase 3.
**Reason:** No phase 3 diff touches comment insertion, blur-commit, Esc-revert, or delete code paths.

### Invariant 7 — Raw mode shows plain comment text and continues to render margin icons
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:1339-1347` — raw mode branch still marks comment ranges with `.mtdComment`/`.mtdCommentLocation` so margin icons draw.
- `LineNumberRulerView` (lines 973-1050) is a separate `NSRulerView` subclass and does not interfere with the text view's draw cycle or comment attributes.
**Reason:** Raw mode comment handling is unchanged; the new ruler occupies a separate view with no intersection.

### Invariant 8 — Comments inside triple-backtick fences are excluded from parsing, hiding, and icon-marking
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:46-92` — `MTDComment.parse` uses `findFenceRanges` to exclude comments inside fences; this is unchanged.
- Phase 3's `applyTables` skips table ranges that intersect fences (line 1742), but does not change comment attribute logic.
**Reason:** Fence exclusion in both comment parser and highlighter is unmodified by phase 3.

### Invariant 9 — Scroll position is preserved across `applyHighlighting` calls
**Verdict:** PASS
**Evidence:**
- `ContentView.swift:1318-1363` — `savedScroll` / `setBoundsOrigin` scroll preservation wraps the entire `storage.beginEditing()...endEditing()` block, including all phase 3 additions (`applyTables` at line 1481 and path/tag coloring at lines 1681-1691).
**Reason:** Phase 3 attributes are applied inside the same guarded `beginEditing` block; the scroll restore runs unconditionally afterward.

### Invariant 10 — Theme cycling and font-zoom continue to work
**Verdict:** PASS
**Evidence:**
- `markthisdownApp.swift:71-113` — `ThemePalette` struct has new fields (`pathColor`, `tagColor`, `tableBorderColor`, `lineNumberColor`) added to both `lightPalette` and `darkPalette`; all pre-existing fields and cycling logic are unchanged.
- `ContentView.swift:1209-1229` — `applyAppearance` is unchanged; zoom scaling path at lines 1204-1207 is unaffected.
**Reason:** New palette fields are additive; existing theme cycling, zoom, and appearance application are untouched.

### Provisional invariant — Comment parse is synchronous on every `document.text` change
**Verdict:** PASS
**Evidence:** `ContentView.swift:268-269` — the single `onChange(of: document.text)` handler still calls `MTDComment.parse(newText)` synchronously. Phase 3 does not introduce any async parse path.
**Reason:** Phase 3 adds only rendering code; the synchronous parse invariant is maintained.

## Summary
Phase ACs: 8, PASS: 8, GAP: 0, UNCLEAR: 0
Invariants checked: 10 (plus 1 provisional), regressions detected: 0
