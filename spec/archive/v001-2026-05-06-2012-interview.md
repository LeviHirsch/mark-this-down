# markthisdown — Interview v1 (adopted)

Date: 2026-05-06
Mode: adopted (iteration 1)
Rough spec source: DEVLOG.md, V2-PLAN.md, README.md, conversation/001-build-markthisdown-md-editor.md
Explore focus: entire project directory

---

## Adopted context (pre-interview)

_Synthesized by Explore sub-agent at adoption time. The standard Socratic interview
will ratify, correct, or extend this content before `/spec seed` runs._

### Shipped reality `[from-code]`

- **Main app entry**: `MarkThisDownApp.swift:107` — `@main` DocumentGroup-based SwiftUI app with AppStorage for theme and fontScale, notification-driven commands for toggle/zoom/comment/sidebar actions (⌘E, ⌘L, ⌘=, ⌘-, ⌘0, ⌘', ⌘\)
- **Document model**: `markthisdownDocument.swift:4-28` — `FileDocument` supporting both `.md` (net.daringfireball.markdown) and plain-text read, UTF-8 write-only to `.md` extension; no autosave config in code, AppKit-supplied
- **Core editor**: `ContentView.swift:96-298` — binds to document text, manages DisplayMode (rendered/raw), SaveState enum (untitled/autosaving/saved), toolbar with 5 left buttons + 5 right buttons, status subtitle auto-updates, debounced 1.5s save-state transition
- **Comments data model**: `ContentView.swift:26-92` — `MTDComment` struct parsed via regex `<!--[\s\S]*?-->` excluding ranges inside triple-backtick fences; stores id (location), range, body (trimmed inner text), lineNumber, contextLine (≤80 chars); reparse on every text change
- **Comments sidebar**: `ContentView.swift:302-400` — right-side inspector (240–420pt wide, toggled ⌘\), displays comment cards sorted by document position, search filter by body+contextLine (case-insensitive), "+" button calls `triggerAddComment`, per-card delete button
- **Comment card UI**: `ContentView.swift:404-520` — shows "Line N", editable body TextField (1–12 lines, axis: .vertical), delete button, context-line preview, background highlight when focused (accent color 0.12 normal / 0.6 border when focused), Enter commits, Shift/Cmd+Enter newlines, Esc reverts + blur-commits
- **NSTextView subclass**: `ContentView.swift:524-896` — `ReadingTextView` with reading-width cap (760pt max), centered margins (basePadding 28pt + reserve 44pt for comment icons), full-width text container sizing; custom draw for HR (1pt full-width line), quote backgrounds (4% tint), quote bars (3pt left, secondary color), bullets (•, secondary-colored, hidden `-/*/+` markers), comment margin icons (text.bubble SF Symbol, 22pt, secondary-label tint, drawn outside text container clip via NSBezierPath clip expansion)
- **Comment insertion logic**: `ContentView.swift:815-895` — `mtdInsertCommentAction` selector; empty selection mid-line → inline `<!--  -->`; empty selection on structural line (heading/list/quote) → block-above with newline; selection without newline → inline after selection; selection with newline → block-above; cursor lands at position 5 (inside spaces); posts `mtdCommentAdded` notification with location
- **Comment margin interaction**: `ContentView.swift:586-655` — `mouseDown` override hit-tests right-margin band (rightX to rightX+marginIconReserve+8), calls `commentLocationForMarginIcon` to find comment at clicked line via `.mtdComment`/`.mtdCommentLocation` attributes; right-margin click → first comment fallback; `hitTest` override claims all bounds to force mouseDown firing
- **Markdown rendering**: `ContentView.swift:1173–1459` (SyntaxHighlighter) — regex-based, cursor-aware; frontmatter (italic gray), fenced code (tinted bg + monospace), HR (hidden text + full-width black line), blockquotes (14pt indent + tinted bg + 3pt bar + `>` marker hidden if cursor off line), bullets (hidden marker + drawn •), headings (h1–h6 sizing, scales with zoom, `#` hidden if cursor off line, bold font), bold (`**...**` with `**` hidden unless cursor on span), italic (`*...*` and `_..._` with markers hidden unless cursor on), inline code (backtick-quoted, monospace + tinted bg, markers hidden), markdown links (`[text](url)` with text colored, url muted), bare URLs via NSDataDetector
- **Comment hiding in render**: `ContentView.swift:1193-1217` — HTML comment ranges set `.foregroundColor` to clear + negative `.kern` to collapse width; if standalone line, sets paragraph `.maximumLineHeight`/`.minimumLineHeight` to 0.01 (no blank space left)
- **Scroll preservation**: `ContentView.swift:1114-1167` — saves clip view origin before `setAttributes` on full storage, restores via `setBoundsOrigin` + `reflectScrolledClipView` after; prevents layout invalidation from nudging visible area during highlight recompute
- **Smart text replace**: `ContentView.swift:971-1003` — `smartReplace` static helper finds common prefix/suffix between old and new text, replaces only the changed range via `textStorage.replaceCharacters` (bypasses textDidChange, preserves cursor/scroll/selection/focus)
- **Theme system**: `markthisdownApp.swift:5-102` — `ThemePalette` struct with 10 colors + 3 fonts + heading sizes array; `AppTheme` enum with system/light/dark cases, `.next` cycles theme, `.palette(systemIsDark:)` returns appropriate palette
- **Installed location**: per README, builds to `~/Applications/MarkThisDown.app` via `scripts/install.sh` using Release config + LaunchServices re-registration; app name no spaces (MarkThisDown); alias `mtd` added to ~/.zshrc if not present
- **Version**: toolbar shows "v2.0.12 · 100%" (version from Info.plist CFBundleShortVersionString, zoom % from fontScale AppStorage), double-click resets zoom to 1.0

### Rough-spec claims `[from-rough-spec]`

- V2 is focused on HTML comment support (sidebar, margin icons, smart insertion) with no sigil requirement — all comments are first-class (from V2-PLAN.md)
- Selection-aware insertion behavior: empty selection → inline, selection with newline → block-above, selection without newline → inline-after (from V2-PLAN.md + conversation)
- Comments are stored as plain HTML `<!-- -->` in the document for LLM compatibility; no custom format or companion files (from conversation/001, V2-PLAN.md)
- Comment count parses on every keystroke and is fine for hundreds but would need debouncing for thousands (from DEVLOG.md)
- Sidebar search is minimally useful and should be considered for removal or making it actual in-document search (from DEVLOG.md)
- Comment edit body field doesn't auto-focus reliably when created via ⌘'; sometimes needs a click (from DEVLOG.md)
- Block comment standalone-line collapse works but cursor lands in invisible character range, viable but weird (from DEVLOG.md)
- Next priorities in order: (1) file-path/bracket-tag coloring, (2) sidebar-card-jumps-to-comment, (3) outline view, (4) frontmatter collapse, (5) tables, (6) code-block syntax highlighting (from DEVLOG.md)
- Things considered and declined: editing comments in margin popover (rejected—sidebar-only simpler), `@`-sigil for typed comments (declined—prefix conventions like TODO:/CITE: for future), custom `.mtd` file extension (declined—plain `.md` preserves AI-readability), Brew tap (discussed not built), git remote (not pushed anywhere, local only) (from DEVLOG.md)

### Ambiguities and tensions

- **Version mismatch**: Info.plist shows CFBundleShortVersionString as "1.2" (line 12) but DEVLOG.md says "v2.0.12" is shipping and claims major v2 changes (comments, sidebar, icons) are in. The code clearly has all v2.0 features; Info.plist is stale and needs updating.
- **Comment parsing overhead**: Code recomputes `MTDComment.parse(document.text)` on every text keystroke (via `onChange` on document.text in ContentView line 242) with no debounce. DEVLOG.md acknowledges this could be a problem at >500 comments but calls it "fine for hundreds." No measured threshold defined.
- **Sidebar search scope**: V2-PLAN.md says search filters comments by text content (rows 63–64), conversation suggests making it "actual document search" (conversation line 336). Code implements the former (search comment body + context line only, not whole doc). Intention unclear.
- **Auto-focus on new comment field**: DEVLOG.md line 67 lists as "worth a focused pass" but code handles it indirectly via `@FocusState` and `onChange(of: isFocused)` (ContentView.swift lines 412, 492–494). Works but only when sidebar is already visible; if sidebar opens async, timing might be loose.
- **Margin icon click behavior**: Code falls back to opening sidebar with first comment if precise hit-test fails (line 595–610). DEVLOG.md v2.0.10–11 entries describe hit-test issues and eventual success but don't document the fallback as intended design or debugging artifact.
- **Comment insertion plan for structural lines**: Code detects `#`, `-/*/+`, `>`, `\d+\.` as structural (line 860) and inserts block-above. V2-PLAN.md (lines 34–41) shows the 4-case table but doesn't specify the regex; rough spec and code agree, but exact pattern is only in source, not spec.
- **Frontmatter multi-line support**: Conversation mentions "having empty is good but also definitely good if it can be multi line in practice" (conversation line 250). Code inserts `description: |` but no collapse feature exists (DEVLOG.md line 71 lists "Frontmatter cannot be collapsed" as known issue). No spec of when/how to edit multi-line descriptions.
- **Comment edit commit semantics**: Code uses `onChange(of: bodyFocused)` to detect blur and write (line 495–501). Multiple events fire: blur → focused=false → onUpdate called. No spec of partial edits, concurrent sidebar + raw-mode editing, or undo behavior.
- **Raw mode comment display**: Code still marks comment ranges with `.mtdComment` and `.mtdCommentLocation` attributes in raw mode (lines 1144–1149) so margin icons render, but text is not hidden. No spec says whether raw mode should show markers or not, or if raw is "plaintext viewable, edit anywhere" vs "plaintext but comments still hidden."
- **Code fence exclusion**: Code searches for triple-backtick pattern with `.anchorsMatchLines` option (line 71), excluding comments inside fences from hiding/listing/icon-marking. No spec clarifies: what if fence is indented? What if backticks are in a comment (e.g., `` <!-- ` --> ``)? What if the comment delimiter itself appears as literal text in rendered code (e.g., user teaching about HTML comments)?

### Suggested axes for interview to confirm

- **Motivation**: Is v2.0.12 shipped and stable, or is this a request to lock in spec for the next iteration (v2.1 / v3)? What is the user trying to accomplish next?
- **Invariants**: Which behaviors *must not change* when iterating? (E.g., "comments stay as plain `<!-- -->`, sidebar optional, margin icons always visible," etc.)
- **Change delta**: Are we validating the existing v2.0 behavior as-is, or ratifying a *different* v2 design than what shipped? (E.g., if Info.plist should say 2.0.12, is that the only fix, or are there design changes?)
- **Scope boundary**: For next iteration, what is explicitly out of scope? (E.g., "no threaded comments," "no typed comment prefixes yet," "outline view deferred to v2.1")
- **Success criteria**: How will the user know this iteration is "done"? (E.g., "comments work smoothly for 100+ comments," "margin icons always clickable," "edit-on-blur never loses data")
- **Outstanding decisions**: Should sidebar search be narrowed to in-document search or removed? Should auto-focus on new comments be guaranteed or acceptable as-is? Should raw mode show or hide comment delimiters?
- **Performance thresholds**: At what comment count should debouncing be mandatory? How many comments per document is expected typical use?
- **File-path / tag coloring**: DEVLOG priority #1 is "file-path and `[bracket-tag]` coloring." Is this a hard requirement for next iteration, or aspirational? What defines a "file path" (regex, extension check)?

---

## Socratic interview

### Phase A — Ratify current state

**Resolutions:**
- A1 (a): Info.plist stale — version is 2.0.12. `[build-change-todo]` Bump CFBundleShortVersionString.
- A2 (c): Double-parse-per-render is the real bug, not a debounce threshold. `[build-change-todo]` Promote `comments` to `@State`, single `onChange` update. Debounce deferred pending post-fix testing.
- A3 (c): Sidebar comment filter is useless — remove it. In-document search deferred. `[build-change-todo]` Remove search bar from CommentsSidebar.
- A4 (b): ⌘' auto-focus is a real bug. `[build-change-todo]` Fix focus timing after comment insertion.
- A5 (b): First-comment fallback on failed hit-test is wrong — no-op is correct. `[build-change-todo]` Remove fallback from commentLocationForMarginIcon.
- A6 (partial): Structural-line block-above insertion is shipped behavior but correctness for list items is TBD. Design question deferred to Phase B.
- A7 (b): Frontmatter collapse is a known gap, in scope this iteration. `[build-change-todo]` Implement frontmatter collapse/expand in ReadingTextView.
- A8 (a): Blur-commit is correct. Edge cases theoretical, not in scope.
- A9 (a): Raw mode shows plain comment text + margin icons. Current behavior is intentional.
- A10 (deferred): Code fence edge cases deferred. Frontmatter as exclusion zone is in scope. `[build-change-todo]` Block ⌘' insertion when cursor is inside frontmatter block.

**Ratified snapshot confirmed by user.**

### Phase B — Intent for this iteration

**Motivation:** Adopting spec workflow to bring systematic discipline to development — clean foundation for future feature additions. No specific deadline or bug trigger.

**Change delta:** Make the app work correctly as a daily markdown viewer/editor. Fixes + full markdown surface coverage + comment workflow reliability, delivered in phases.

**Must-have (app correctness):**
- All Phase A build-change-todos (7 items)
- Tables rendering
- File-path and `[bracket-tag]` coloring
- Code-block syntax highlighting
- Sidebar card click → jump to comment location in editor

**Near-core:** Frontmatter collapse (already in Phase A todos)

**Feature addition:** Outline view — left toggle (button or side-panel, design deferred to implementation), shows headings only, saves + restores scroll position on toggle back.

**Invariants:** Comments stay as plain `<!-- -->`; `.md` extension only; no companion files; document remains LLM-readable without custom tooling.

**Scope boundary (explicit out):** Threaded comments, `@`-sigil typed comments, custom `.mtd` extension, Brew tap, git remote, cloud sync.

**Success criteria:** All phases implemented; app handles full common markdown surface without visual gaps; comment workflow (insert, edit, delete, navigate) is reliable and snappy.

**List item insertion semantics:** Open design question — block-above may break list continuity; inline may be preferable. To be resolved as a `/spec decide` during seed or implementation.

## Conclusion

- **Goal:** A macOS markdown viewer/editor where the app works correctly — full markdown surface coverage, reliable comment workflow, no visual gaps.
- **Constraints:** macOS only; SwiftUI + AppKit; `.md` files only; comments as plain `<!-- -->`; no companion files; LLM-readable document format.
- **Success criteria:** All iteration phases shipped; full markdown surface renders correctly; comment insert/edit/delete/navigate is reliable and snappy in daily use.
- **Scope boundary:** No threaded comments, no `@`-sigil, no `.mtd` extension, no Brew tap, no cloud sync.
- **Root need:** A focused, distraction-free markdown editor with first-class annotation support — not a symptom fix.
- **Delta:** Fix known bugs + cover the full markdown viewing surface + add outline view, in phases.
- **Invariants:** Comments as `<!-- -->`; `.md` only; no companion files; LLM-readable format; existing comment insert/edit/delete UX preserved.

