# Implement audit — iteration 1 — phase 3 — 2026-05-15-2222
> spec.md SHA: 53303c199ff28fd605447f1e9f64bd69723f879f
> Phase scope: AC9.1, AC9.2, AC10.1, AC10.2, AC10.3, AC11.1, AC11.2, AC11.3, AC11.4, AC11.5, AC21.1, AC21.2, AC21.3, AC21.4, AC21.5

## AC9.1 — GFM pipe table renders with visible column separators and header styling in rendered mode
**Verdict:** PASS
**Evidence:** ContentView.swift:2105–2156 (`styleTable`); ContentView.swift:920–945 (`drawTablePipes`); ContentView.swift:2117–2122 (bold header row); ContentView.swift:2113–2115 (tinted background)
**Reason:** Separator row is detected, header gets bold font, body rows get tinted background, and `|` glyphs are replaced by drawn 1pt vertical lines via `drawTablePipes`.

## AC9.2 — Table source is hidden or styled in rendered mode consistent with other block elements
**Verdict:** PASS
**Evidence:** ContentView.swift:2114–2115 (monospace + codeBackground tint on full table range); ContentView.swift:2126–2135 (separator row collapsed to 0.01 lineHeight + clear foreground + negative kern); ContentView.swift:2148–2152 (`|` glyphs hidden with clear foreground and flagged for custom draw)
**Reason:** Table source is styled (not raw plain text) in rendered mode: tinted background throughout, separator row collapsed to a hairline, pipe glyphs hidden and replaced by drawn lines — consistent with code-block treatment.

## AC9.3 — Raw mode shows plain table source unchanged
**Verdict:** PASS
**Evidence:** ContentView.swift:1646–1667 (`applyHighlighting` branches: `SyntaxHighlighter.apply` called only in rendered mode; raw mode applies no table styling)
**Reason:** Raw mode skips `SyntaxHighlighter.apply` entirely, so table source is displayed as plain text with no modifications.

## AC10.1 — Bare file paths are colored distinctly in rendered mode
**Verdict:** PASS
**Evidence:** ContentView.swift:2021–2028; pattern `(?<![\w./~-])((?:\.{1,2}/|~/|/)[A-Za-z0-9_./~\-]+|[A-Za-z0-9_~][A-Za-z0-9_./~\-]*\.[A-Za-z0-9]{1,6})\b`; markthisdownApp.swift:83,106 (`pathColor` in both palettes)
**Reason:** Pattern covers starts-with `./`, `../`, `~/`, `/` and ends-with-extension (.ext up to 6 chars); colored with `palette.pathColor` which is defined distinctly in both light and dark palettes.

## AC10.2 — `[bracket-tag]` tokens are colored distinctly
**Verdict:** PASS
**Evidence:** ContentView.swift:2031–2034; pattern `\[([^\]\n]+)\](?!\()` with `palette.tagColor` applied to inner group (not brackets); markthisdownApp.swift:84,107 (`tagColor` in both palettes)
**Reason:** Pattern matches `[text]` not followed by `(`, applied to the inner text only, colored with `palette.tagColor`; the negative lookahead correctly excludes Markdown links.

## AC10.3 — Coloring does not apply inside fenced code blocks or frontmatter
**Verdict:** PASS
**Evidence:** ContentView.swift:2004–2019 (`pathTagSkip` closure); ContentView.swift:2012–2013 (`isInFenceOrFrontmatter` checked as first condition); ContentView.swift:1795–1807 (frontmatter range detection); also skips comments, inline-code spans, MD links, bare URLs
**Reason:** `pathTagSkip` checks `isInFenceOrFrontmatter` (covering both fenced code blocks and frontmatter) before applying any coloring; matches AC10.3's requirement.

## AC11.1 — Reserved left margin displays line numbers for each logical line in raw mode
**Verdict:** PASS
**Evidence:** ContentView.swift:1120–1281 (`LineNumberGutterView`); ContentView.swift:1338–1348 (gutter created and added via `addFloatingSubview`); ContentView.swift:1345 (`gutter.drawsLabels = (mode == .raw)`)
**Reason:** `LineNumberGutterView` is created at editor initialization and draws labeled line numbers when `drawsLabels` is true (raw mode only).

## AC11.2 — Wrapped lines: only first visual row is numbered; continuation rows show no number
**Verdict:** PASS
**Evidence:** ContentView.swift:1240–1242 (comment: "Only the first line fragment of each paragraph gets a number — wrapped continuation rows are skipped (AC11.2)"); ContentView.swift:1243–1278 (paragraph-level iteration using `nsString.lineRange`, advancing by full paragraph to skip wrap fragments; only one label drawn per logical line)
**Reason:** The draw loop iterates by `lineRange` (logical paragraph), drawing one label per paragraph at the first line fragment's y-coordinate; no label is drawn for wrapped continuation rows.

## AC11.3 — Line numbers are not visible in rendered mode
**Verdict:** PASS
**Evidence:** ContentView.swift:1131–1133 (`drawsLabels: Bool` property; when false "skips drawing the numbers themselves (AC11.3 + AC11.4)"); ContentView.swift:1411 (`gutter.drawsLabels = (mode == .raw)`); ContentView.swift:1401–1403 (comment: "Line-number visibility tracks raw mode (AC11.3)")
**Reason:** `drawsLabels` is set to `mode == .raw`, so rendered mode causes the gutter to skip all label drawing while retaining its reserved width.

## AC11.4 — Gutter reserved width is preserved in rendered mode; toggling modes does not horizontally reposition document text
**Verdict:** PASS
**Evidence:** ContentView.swift:1116–1119 (comment: "Its width is also the floor for the text view's `textContainerInset.width`, so toggling `drawsLabels` (raw vs. rendered) preserves the text's horizontal position (AC11.4)"); ContentView.swift:673–680 (`gutterFloor = gutter?.gutterWidth ?? basePadding`; `leftPad = max(gutterFloor, slack / 2)`) — gutter width is always the minimum left inset regardless of mode; ContentView.swift:1401–1403 (inline comment citing AC11.4)
**Reason:** `updateReadingMargins` uses `gutter.gutterWidth` as the floor for `textContainerInset.width` in both modes, so the text origin never shifts when toggling.

## AC11.5 — Gutter positioned relative to left edge of text content area, not window or scroll-view edge
**Verdict:** PASS
**Evidence:** ContentView.swift:1183–1198 (`syncFrame`); ContentView.swift:1192–1196 (`x = clipFrame.minX + leftPad - gutterWidth` — `leftPad` is `tv.textContainerInset.width`, so gutter sits immediately left of text content area); ContentView.swift:1114–1116 (comment: "sits immediately to the left of the text content area (AC11.5)")
**Reason:** `syncFrame` positions the gutter by computing the text-content-area's x coordinate (`clipFrame.minX + textContainerInset.width`) and subtracting `gutterWidth`, placing it flush against the text content regardless of window width.

## AC21.1 — Raw mode: tint band across full text content width for caret's logical line; tint = body color at 4% alpha
**Verdict:** PASS
**Evidence:** ContentView.swift:700–723 (`drawCurrentLineHighlight`); ContentView.swift:719–723 (band rect spans `tc.size.width` from `origin.x`; filled with `currentLineHighlightColor`); ContentView.swift:1404 (`highlightTint = palette.bodyColor.withAlphaComponent(0.04)`); ContentView.swift:1405 (`tv.currentLineHighlightColor = highlightTint`)
**Reason:** Band is drawn across the full text container width, using `bodyColor` at alpha 0.04 (4%), matching the spec exactly.

## AC21.2 — Raw mode: gutter row aligned with caret's logical line renders with matching tint band
**Verdict:** PASS
**Evidence:** ContentView.swift:1256–1261 (gutter `draw`: `if isCurrent { band = NSRect(x: 0, y: yInGutter, width: bounds.width, height: fragRect.height); currentLineHighlightColor.setFill(); band.fill() }`); ContentView.swift:1410 (`gutter.currentLineHighlightColor = highlightTint`) — same tint as text body
**Reason:** The gutter's `draw` method paints a full-width tint band at the y-position of the current line, using the same `currentLineHighlightColor` set to body color at 4% alpha.

## AC21.3 — Raw mode: gutter label for caret's logical line renders in bold
**Verdict:** PASS
**Evidence:** ContentView.swift:1230–1233 (`boldFont = NSFontManager.shared.convert(rulerFont, toHaveTrait: .boldFontMask)`; `currentAttrs` uses `boldFont`); ContentView.swift:1264 (`attrs = isCurrent ? currentAttrs : normalAttrs`); commit 26ffe88 (also adds `chosenLineNumberColor` as the foreground for current-line label)
**Reason:** The current-line label is drawn with bold font weight (and a distinct foreground color `chosenLineNumberColor`) while all other labels use the regular-weight `rulerFont`.

## AC21.4 — Rendered mode: no current-line highlight shown
**Verdict:** PASS
**Evidence:** ContentView.swift:1563 (`rtv.currentLineHighlightEnabled = raw`) — disabled when not in raw mode; ContentView.swift:1567 (`gutter.currentLineNumber = raw ? line : 0`) — gutter uses 0 (no highlight) in rendered mode; ContentView.swift:701 (`guard currentLineHighlightEnabled` — body highlight skipped when disabled)
**Reason:** Both the text-body highlight (`currentLineHighlightEnabled = raw`) and the gutter tint (`currentLineNumber = raw ? line : 0`) are suppressed in rendered mode.

## AC21.5 — Current-line highlight only active when text view is first responder; no highlight when focus is elsewhere
**Verdict:** GAP
**Evidence:** ContentView.swift:1547–1569 (`syncCurrentLine`): `rtv.currentLineHighlightEnabled = raw` — no first-responder check; ContentView.swift:1538–1541 (`textViewDidChangeSelection` triggers `syncCurrentLine`); no override of `becomeFirstResponder`/`resignFirstResponder` or NSWindow key-change notifications found anywhere in ContentView.swift
**Reason:** `currentLineHighlightEnabled` is set based solely on `mode == .raw` with no check for whether the text view is first responder; the highlight will remain visible when focus moves to the sidebar or toolbar while in raw mode.

---

## Invariant regression check

### Invariant 1 — Comments stored as plain `<!-- -->` HTML; no custom format, no companion files
**Verdict:** skipped — orthogonal to phase scope

### Invariant 2 — Document extension is `.md` only
**Verdict:** skipped — orthogonal to phase scope

### Invariant 3 — Document remains LLM-readable without custom tooling
**Verdict:** skipped — orthogonal to phase scope

### Invariant 4 — Autosave is AppKit-supplied; no custom save path
**Verdict:** skipped — orthogonal to phase scope

### Invariant 5 — Comment margin icons visible in rendered mode alongside every comment
**Verdict:** PASS
**Evidence:** ContentView.swift:1702–1707 (`SyntaxHighlighter.apply` tags every comment range with `.mtdComment`/`.mtdCommentLocation`); ContentView.swift:948–1024 (`drawCommentMarginIcons` draws in both modes); ContentView.swift:1659–1665 (raw mode also sets `.mtdComment` attributes)
**Reason:** Phase 3 changes (gutter, table, coloring) do not touch the comment icon draw path; margin icons remain functional in both modes.

### Invariant 6 — Existing comment insert/edit/delete UX preserved
**Verdict:** skipped — orthogonal to phase scope

### Invariant 7 — Raw mode shows plain comment text and continues to render margin icons
**Verdict:** PASS
**Evidence:** ContentView.swift:1658–1666 (raw mode branch applies only `.mtdComment`/`.mtdCommentLocation`; no styling applied to comment text); ContentView.swift:948–1024 (`drawCommentMarginIcons` runs in raw mode)
**Reason:** Phase 3 did not alter raw mode's comment rendering path; plain text and margin icons are preserved.

### Invariant 8 — Comments inside triple-backtick fences excluded from parsing, hiding, and icon-marking
**Verdict:** skipped — orthogonal to phase scope

### Invariant 9 — Scroll position preserved across `applyHighlighting` calls
**Verdict:** PASS
**Evidence:** ContentView.swift:1637–1680 (`applyHighlighting` saves `savedScroll` before `storage.beginEditing()` and restores via `clip.setBoundsOrigin(savedScroll)` after `storage.endEditing()`)
**Reason:** The save/restore mechanism was not disturbed by Phase 3 changes; gutter's `syncFrame` is called from `updateReadingMargins` (layout-driven) not from inside `applyHighlighting`.

### Invariant 10 — Theme cycling and font-zoom continue to work
**Verdict:** PASS
**Evidence:** ContentView.swift:1339–1342 (`gutter.lineNumberColor = palette.lineNumberColor`; `gutter.chosenLineNumberColor = palette.chosenLineNumberColor`; `gutter.backgroundColor = palette.background` set on `makeNSView`); ContentView.swift:1406–1411 (same fields updated in `updateNSView` on every SwiftUI state change including theme/zoom); markthisdownApp.swift:83–87,106–110 (pathColor, tagColor, lineNumberColor, chosenLineNumberColor defined in both light and dark palettes)
**Reason:** Phase 3 added new palette fields (pathColor, tagColor, lineNumberColor, chosenLineNumberColor) and all are consistently updated in both `makeNSView` and `updateNSView`, so theme cycling applies them correctly.

### Provisional Invariant 1 — Comment parse is synchronous on every `document.text` change
**Verdict:** skipped — orthogonal to phase scope

### Provisional Invariant 2 — Structural-line block-above insertion applies to list items
**Verdict:** skipped — orthogonal to phase scope

---

## Summary
Phase ACs: 15, PASS: 14, GAP: 1, UNCLEAR: 0
Invariants checked: 4 (of 10 total; 6 skipped as orthogonal), regressions detected: 0
